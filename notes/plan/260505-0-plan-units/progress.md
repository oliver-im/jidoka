# 260505-0-plan-units — Progress

**Cursor:** all units complete; ready to archive.

## Done

- **01 — Housekeeping & scaffolding.** Updated repo-state section in root AGENTS.md; bumped Cargo.toml to 0.2.0 and dropped the v0.2.4 banner from `docs/developer-guide.md`; re-framed README to lead with plan-mode dir materialization (topology demoted to second-line, plus a "Direct Topology Rendering" subsection for the standalone CLI path); created `notes/plan/AGENTS.md` (resume protocol); rewrote `notes/AGENTS.md` to the new YYMMDD-N-slug shared-counter convention with an updated archiving rule. `cargo build --release` + `cargo test` clean (161 tests pass); `planview --version` prints `0.2.0`.
- **02 — Plan + Unit data model.** Added `Plan` and `Unit` types to `src/types.rs` with null-rejecting deserializers for `agents_involved` and `topology`. Extended `ValidationError` with nine plan-specific variants (slug, unit-id format, duplicate, empty title/summary, dangling/self/cyclic blocked_by, empty units) and added `validate_plan` plus `is_valid_slug` / `is_valid_unit_id` helpers in `src/validate.rs`. Per-unit `topology` is delegated to a new private `validate_topology_into(topology, root_path, errors)` so embedded errors carry `units[N].topology.agents…` paths. Added a parallel `detect_unit_cycles` (DFS, three-color) for the unit dependency graph. Tests: 9 new fixture files + 12 plan integration tests in `tests/validation_test.rs` (round-trip, sequential, with-topology, empty-units, bad-slug, bad-unit-id, duplicate, dangling, cycle, self-dep, empty title/summary, topology-error path prefix), plus 12 inline tests for the new helpers and Plan/Unit serde round-trip. Total 186 tests pass.
- **03 — Plan dir materialization.** New `src/materialize.rs` module: `resolve_target_dir(plan, plans_root, today_yymmdd)` picks a fresh `<YYMMDD>-<N>-<slug>` name by scanning `plans_root` plus `research/`, `backlog/`, and `done/plan/` siblings for `^<today>-(\d+)-` entries (shared daily counter per `notes/AGENTS.md`); `materialize` orchestrates and `materialize_at` is the lower-level "write into this exact path" entry point. Per-file writers emit `overview.md` (front-matter + decisions skeleton + unit table with 2-digit-prefixed blocked-by column), `progress.md` (cursor → first unit, empty Done/Blockers, resume-protocol note), and `<unit.id>.md` (title, blocked-by/agents/topology metadata, summary, body_markdown verbatim, optional ```mermaid block via `crate::mermaid::generate`, review checklist). Atomic `.tmp+rename` writes; "target exists" guard on the `_at` entry point. New CLI subcommand `planview materialize <plan.json> [--plans-root] [--today]` resolves project root via `$CLAUDE_PROJECT_DIR` (fallback PWD with stderr warning) and the date via `date +%y%m%d`. 8 new in-module tests + 6 integration tests in `tests/materialize_test.rs` exercising the full pipeline against the unit-02 fixtures. Manual smoke: ran `planview materialize tests/fixtures/valid_plan_with_topology.json --today 260505` against a tempdir; all four files emitted, mermaid block well-formed. Total 200 tests pass.
- **04 — HTML overview rendering.** New `templates/plan.html` template + `render_plan_html(plan, dir_name) -> String` in `src/html.rs`. Page is sticky-TOC + per-unit cards with chips (`Blocked by`, agents-involved, optional `topology` badge), unit summary, body markdown rendered client-side (marked + DOMPurify), per-unit `pre.mermaid` blocks for any embedded topology, review-step checklist, and a footer back-link to overview. The overview-card content is the materialized `overview.md` re-rendered live by marked. Refactored `src/materialize.rs` to expose pure builders (`build_overview_md`, `build_progress_md`, `build_unit_md`); `write_*_md` are now thin wrappers and `write_plan_html` lands `<plan-dir>/overview.html`. Wired the CLI subcommand to write HTML and open the browser unless `PLANVIEW_NO_OPEN` is set. Extended `static/style.css` (~200 lines for the plan view) and `static/script.js` (`initPlanViewRendering` + a `renderMarkdownInto` helper). Hardened JSON-in-`<script>` payloads with a new `json_for_script` helper that rewrites `</` and `<!--` so user-controlled markdown can't break out of the script tag — applied to both the new plan view and the existing topology `--plan` path. 7 new in-module tests (basic structure, anchors+blocked-by chips, topology mermaid block, JSON-encoded markdown, CDN deps, review-step rendering, title escaping, `</script>` defense in unit body). Manual smoke: ran the CLI against `valid_plan_with_topology.json`; got an 860-line `overview.html` with embedded CSS/JS and correctly-rendered structure. Total 208 tests pass.
- **05 — Hook integration update.** Rewrote `src/hook.rs` end-to-end. Hook now reads `/tmp/planview-{session_id}.json` as a `Plan`; on file-missing or wrong-shape (e.g. a stale Topology JSON), it falls through to the standard "run /planview" deny — same `BLOCKED…` message and `MAX_DENY_ATTEMPTS=3` cap. On a valid Plan, it runs `validate_plan` (errors emit a deny carrying the validation messages, no marker bump), resolves `<project>/notes/plan/<YYMMDD>-<N>-<slug>` via `resolve_target_dir`, refuses to clobber an existing target with a slug-specific deny message, then calls `materialize_at` + `write_plan_html`, clears the marker, and opens the browser unless `PLANVIEW_NO_OPEN` is set. Project root resolves via `$CLAUDE_PROJECT_DIR` with a stderr-warned PWD fallback (claude-code issue #22343). Dropped the topology-rendering and `scan_plans_dir` paths — the hook is now Plan-only; the standalone CLI keeps the topology surface unchanged. `Config` carries `today_yymmdd` so tests can inject a fixed date. 8 of 19 hook tests are new/rewritten: `valid_plan_materializes_and_writes_html` (success path + marker cleared), `topology_in_tmp_treated_as_missing_plan` (deny + no plan dir), `validation_error_emits_deny_with_message` (no plan dir, no marker bump), `target_dir_exists_emits_deny` (no clobber). All 207 tests pass; the always-exit-0 invariant is preserved end-to-end. **Deferred to user:** end-to-end manual smoke in a real Claude Code session (enter plan mode → run /planview → exit; and the no-/planview deny-then-retry flow). The skill itself is rewritten in unit 06; without it the smoke isn't fully runnable yet.
- **06 — Skill rewrite + agent-guide.** Rewrote `.claude/skills/planview/SKILL.md` end-to-end: description now frames plan-mode unit decomposition (which doubles as the system-prompt fragment Claude sees in plan mode), the data model section embeds a typed Plan/Unit/Topology summary, and a Heuristics block adds explicit guidance on unit-splitting (reviewable on its own + finishable in one session, 3–7 units typical), topology decision (default null, only attach when a unit dispatches >1 agent with meaningful structure, single-agent units never carry topology), slug derivation, sequential `blocked_by` defaults, and `review_steps` defaults (`/code-review:code-review` baseline; add `agent-cli` for foundational/risky units). Hard rules updated: skill saves Plan JSON only, never calls the renderer or generates HTML. Created `.claude-plugin/plugin.json` so the install IS the prompt-injection — no per-project AGENTS.md needed. Rewrote `docs/data-model.md` with a "Plan vs Topology" framing paragraph at the top, full Plan + Unit schema sections mirroring the Topology section style, and a reframing of Topology as per-unit optional. Rewrote `docs/agent-guide.md` Process + Heuristics around Plan with the same heuristics (with concrete examples for unit splitting and the topology decision). Updated `docs/developer-guide.md` end-to-end: new architecture diagram showing Plan flow as primary + topology as legacy/standalone, validation-rules section split into Topology Rules (unchanged numbered list) and Plan Rules (rules 19–28), CLI Interface includes the new `materialize` subcommand with its `--plans-root` and `--today` options, env-var table swaps `CLAUDE_PLANS_DIR` for `CLAUDE_PROJECT_DIR`, plugin manifest section reflects the new `.claude-plugin/plugin.json` shape, hook-integration section rewritten with the Plan-only flow (parse-as-Plan, validate, materialize, write HTML, clear marker, open browser) including the deny taxonomy (missing plan vs validation error vs target-exists) and the marker-only-bumps-on-missing-plan semantic. Dropped the `CLAUDE_PLANS_DIR` reference from the README env-var table too. Skill description loaded successfully into the live skill list (verified mid-session). 207 tests still pass — no Rust changes in this unit.
- **07 — Config workflow.** New `src/config.rs` module: `Config { plan_dir_root, auto_open_browser, html_output, hook_behavior (Deny/Warn/Silent), max_deny_attempts, plan_level_topology }` with `Default` and `serde(default)` so partial JSON fills missing fields with defaults. Layered loader: defaults < global `~/.claude/plugins/planview/config.json` < per-project `<project>/.planview.json` (allows `plan_dir_root` only; warn-and-ignore other keys per claude-hud convention). Invalid JSON at either layer falls back to defaults with a stderr warning — never panics. `merge_for_write` round-trips the global file while preserving manually-added keys for `planview:configure`. Wired Config through `src/hook.rs`: hook now uses `config.plan_dir_root` (project-relative resolved against `$CLAUDE_PROJECT_DIR`), respects `config.html_output` (skips `write_plan_html` when false), `config.auto_open_browser` (env `PLANVIEW_NO_OPEN` still wins for safety), `config.hook_behavior` (Deny = today; Warn = log to stderr and let ExitPlanMode through; Silent = behave as `PLANVIEW_NO_AUTO`), and `config.max_deny_attempts` (replaces the hardcoded 3). Same wiring on the `planview materialize` CLI subcommand. Added two new user-facing skills under `.claude/skills/`: `setup` (interactive walkthrough, write `~/.claude/plugins/planview/config.json` from scratch) and `configure` (diff-style edits, preserves manually-added keys). Tests: 8 in-module config tests + 7 file-bound integration tests in `tests/config_test.rs` (defaults / global only / global+project / invalid global / invalid project / manual-edit preservation / round-trip), plus 4 new hook tests for the new config knobs (Warn doesn't bump marker, Silent doesn't bump marker, html_output=false skips overview.html, max_deny_attempts honored). Manual smoke: ran `planview materialize` against a tempdir with a global config setting `html_output=false` + a project override setting `plan_dir_root=custom-plans` — the dir landed at `custom-plans/260505-0-tidy-readme/` (project override won) with no overview.html (global html_output respected). All 226 tests pass. **Deferred to user:** end-to-end manual smoke of the two skills in a real Claude Code session (interactive Q&A flows can't be exercised programmatically); enabling planview in another repo to confirm the plugin manifest auto-loads (depends on the plugin distribution path, which is out of scope for this branch).

## Blockers

_None._

## Notes

- Plan was hand-bootstrapped (the materialization tool we're building doesn't exist yet). Once unit 03 lands, future plans materialize via the hook.
- When resuming, read this file first to find the cursor unit, then read the cursor unit's md. Skip `overview.md` unless this is the first session on the plan.
