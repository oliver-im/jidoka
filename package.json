{
  "name": "jidoka",
  "version": "0.4.2",
  "description": "Materialize multi-unit plans on ExitPlanMode",
  "license": "MIT",
  "type": "module",
  "private": true,
  "main": "dist/cli.js",
  "bin": {
    "jidoka": "dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "eta": "^4.6.0",
    "strip-json-comments": "^5.0.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
