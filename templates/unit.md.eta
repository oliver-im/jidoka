# Unit <%= it.prefix %> — <%= it.title %>

**Blocked by:** <%= it.blockedBy %>
**Agents involved:** <%= it.agents %>

## Summary

<%= it.summaryBlock -%>
<%= it.bodyBlock -%>
## Review pipeline

<%= it.reviewItems %>
---
See `progress.md` for the cursor and overall plan state.
