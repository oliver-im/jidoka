# backlog/

Candidate work — what could be picked up (not started; may drift). A **re-derivable catalog** (one line per entry, regenerated by grep — see `../AGENTS.md`), not hand-maintained meta.

_Re-derive: `grep -rhm1 '^# ' --include='[0-9]*.md' docs/exec-plans/backlog/ | sed 's/^# /- /' | sort`_

_No entries yet._
